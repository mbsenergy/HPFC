% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_outlier.R
\name{filter_outlier_dd_pwr}
\alias{filter_outlier_dd_pwr}
\title{Filter Outliers in Daily Power Data}
\usage{
filter_outlier_dd_pwr(DT)
}
\arguments{
\item{DT}{A \code{data.table} containing at least two columns:
\itemize{
\item \code{date}: Date of the observation.
\item \code{value}: The power value associated with each date.
\item \code{break_group_p} (optional): A grouping variable for detected break periods.
}}
}
\value{
A \code{data.table} with the following columns:
\itemize{
\item \code{date}: The original date.
\item \code{value_day}: The daily mean value.
\item \code{out_dummy}: Binary indicator (1 if an outlier, 0 otherwise).
}
}
\description{
This function filters outliers in daily power data using the Hamilton filter. It computes
daily averages, applies a trend filter, and removes values that deviate more than three
standard deviations from the trend.
}
\details{
The function performs the following steps:
\itemize{
\item Computes daily mean values.
\item Applies the \code{neverhpfilter::yth_filter} Hamilton filter to extract the trend.
\item Identifies outliers as values that deviate more than three standard deviations from the trend.
\item Filters out detected outliers from the dataset.
}
}
\examples{
# Example usage with a sample data.table `DT`
DT <- data.table(date = as.Date('2020-01-01') + 0:9, 
                 value = rnorm(10, 100, 10), 
                 break_group_p = sample(1:2, 10, replace = TRUE))
result <- filter_outlier_dd_pwr(DT, "value")

}
