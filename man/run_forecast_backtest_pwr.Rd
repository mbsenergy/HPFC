% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{run_forecast_backtest_pwr}
\alias{run_forecast_backtest_pwr}
\title{Run Power Forecast Backtest}
\usage{
run_forecast_backtest_pwr(
  x,
  in_select_backtest_source = "Last",
  in_select_backtest_period_1,
  in_select_backtest_period_2,
  shiny_sim = NULL,
  reuters_key
)
}
\arguments{
\item{x}{Character. Power market code (e.g., "Greece").}

\item{in_select_backtest_source}{Character. Source of the backtest. One of \code{'Last'} or \code{'Sim'}.}

\item{in_select_backtest_period_1}{Character. Start date of the backtest in \code{'YYYY-MM-DD'} format.}

\item{in_select_backtest_period_2}{Character. End date of the backtest in \code{'YYYY-MM-DD'} format.}

\item{shiny_sim}{Character or NULL. Simulation identifier used if \code{in_select_backtest_source = 'Sim'}.}

\item{reuters_key}{Character. API key required to pull forward data using \code{eikondata}.}
}
\value{
A named list with two elements:
\describe{
\item{\code{dt_pwr}}{\code{data.table} with forecast and observed spot values for each hour.}
\item{\code{dt_pwr_lg}}{\code{data.table} in long format with forecast, spot, and other attributes.}
}
}
\description{
Executes a backtest of power price forecasts using pre-trained models and forward curves.
}
\details{
\itemize{
\item Loads pre-trained models and historical data from either the "last" or "archive" directory structure.
\item Uses the \code{prepare_fwd()}, \code{forecast_gas()}, and \code{forecast_pwr()} functions to generate the backtest.
\item Writes forecast results to disk under the corresponding folder structure.
}
}
\examples{
run_forecast_backtest_pwr(
  x = "Greece",
  in_select_backtest_source = "Last",
  in_select_backtest_period_1 = "2024-01-01",
  in_select_backtest_period_2 = "2024-12-31",
  shiny_sim = NULL,
  reuters_key = "YOUR_KEY"
)

}
